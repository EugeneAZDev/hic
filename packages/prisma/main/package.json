{
  "name": "@hic/prisma-main",
  "version": "0.0.1",
  "private": true,
  "main": "client.ts",
  "types": "client.ts",
  "scripts": {
    "prisma": "prisma",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
